{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@better-comments/api": "workspace:*",
    "@better-comments/auth": "workspace:*",
    "@better-comments/db": "workspace:*",
    "@better-comments/env": "workspace:*",
    "@libsql/client": "0.15.15",
    "@hono/trpc-server": "^0.4.0",
    "@trpc/server": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "hono": "catalog:",
    "libsql": "0.5.22",
    "zod": "catalog:"
  },
  "optionalDependencies": {
    "@libsql/linux-x64-gnu": "0.5.22"
  },
  "devDependencies": {
    "@better-comments/config": "workspace:*",
    "@types/bun": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "catalog:"
  }
}
